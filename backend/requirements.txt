# =============================================================================
# Genba Management System — Backend Python Dependencies
# All versions pinned for reproducibility
# =============================================================================

# --- Core Framework ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12      # Form data parsing

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0               # PostgreSQL async driver
alembic==1.14.0

# --- Validation & Settings ---
pydantic[email]==2.10.0
pydantic-settings==2.6.1

# --- Authentication & Security ---
python-jose[cryptography]==3.3.0   # JWT encode/decode
bcrypt==4.2.1                       # Password hashing (cost factor 12)
passlib[bcrypt]==1.7.4

# --- Cache & Session ---
redis[hiredis]==5.2.0               # Redis async client with hiredis parser

# --- Task Scheduling ---
apscheduler==3.10.4                 # Invoice auto-generation cron

# --- Object Storage (S3-Compatible) ---
boto3==1.35.74
botocore==1.35.74

# --- HTTP Client ---
httpx==0.28.0                       # Async HTTP client (also used in tests)

# --- Utilities ---
python-dateutil==2.9.0.post0
pytz==2024.2                        # Timezone handling

# --- Logging ---
structlog==24.4.0                   # Structured JSON logging

# --- Development & Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-env==1.1.5
factory-boy==3.3.1                  # Test data factories

# --- Code Quality ---
ruff==0.8.1                         # Linting + formatting
mypy==1.13.0                        # Static type checking
openpyxl==3.1.2
